export enum GameInputType {
  INTENT = "intent",
}

export enum GameIntent {
  UP = "up",
  DOWN = "down",
  LEFT = "left",
  RIGHT = "right",
  ACTION = "action",
  PAUSE = "pause",
  RESUME = "resume",
  START = "start",
  RESET = "reset",
}

export enum MobileControlScheme {
  TWO_BUTTON = "two_button",
}
