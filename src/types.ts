export enum GameInputType {
  INTENT = "intent",
}

export enum GameIntent {
  UP = "up",
  DOWN = "down",
  LEFT = "left",
  RIGHT = "right",
  CLOCKWISE = "clockwise",
  COUNTER_CLOCKWISE = "counter_clockwise",
  ACTION = "action",
  PAUSE = "pause",
  RESUME = "resume",
  START = "start",
  RESET = "reset",
}
